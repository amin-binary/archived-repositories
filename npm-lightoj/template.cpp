// AUTHOR
#include <functional>
#include <algorithm>
#include <iostream>
#include <cstring>
#include <iomanip>
#include <string>
#include <vector>
#include <deque>
#include <cmath>
#include <list>
#include <map>
#include <set>

#define rep(i,f,t)  for(int i = f; i < t; ++i)
#define repe(i,f,t) for(int i = f; i <= t; ++i)
#define all(v)      v.begin(), v.end()

using namespace std;
using ii = pair<int, int>;
using vi = vector<int>;
using ll = long long;

void PROB();
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL); PROB(); return 0;
}

void PROB() {
    int T; cin >> T;
    repe(t,1,T) {
        cout << "Case " << t << ": " << 0 << endl;
    }
}

/*******************************
EXPLANATION***/
